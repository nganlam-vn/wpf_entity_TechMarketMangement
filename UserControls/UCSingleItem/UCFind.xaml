<UserControl x:Class="wpf_TechMarketMangement.UserControls.UCSingleItem.UCFind"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:wpf_TechMarketMangement.UserControls.UCSingleItem"
             mc:Ignorable="d" 
             DataContext="{StaticResource FindVM}"
             Name="FindView"
             d:DesignHeight="50" d:DesignWidth="1200">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="450"/>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <TextBox Text="{Binding FilterName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10" BorderBrush="White"/>
        </Grid>
        <Grid Grid.Column="2">
            <Image Source="/UserControls/UCSingleItem/pngwing.com.png" Margin="15,15,15,15" />
        </Grid>
    </Grid>
</UserControl>
